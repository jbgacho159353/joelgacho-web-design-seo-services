# Node dependencies — not for the repo
node_modules/

# Netlify CLI local state (machine-specific, not for the repo)
.netlify/

# Environment variables — never commit secrets
.env
.env.*

# OS / editor cruft
Thumbs.db
.DS_Store
